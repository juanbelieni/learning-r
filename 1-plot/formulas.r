# Usign formulas

plot(cos, 0, 2 * pi)
plot(exp, 1, 5)
plot(dnorm, -3, 3)

# Plot with options
plot(dnorm, -3, 3,
  col = "#cc0000", lwd = 5,
  main = "Standard Normal Distribution",
  xlab = "z-scores",
  ylab = "Density"
)
